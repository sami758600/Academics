class Solution(object):
    def containsDuplicate(self, nums):
        d={}
        flag=False
        for x in nums:
            if x in d:
                d[x]+=1
            else:
                d[x]=1
        
        for x in d:
            if d[x]>=2:
                flag=True
        return flag
