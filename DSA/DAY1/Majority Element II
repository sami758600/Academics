class Solution(object):
    def majorityElement(self, nums):
        d={}
        r=[]
        n=len(nums)//3
        for x in nums:
            if x in d:
                d[x]+=1
            else:
                d[x]=1
        
        for  x in d:
            if d[x]>n:
                r.append(x)
        return r
        

        
